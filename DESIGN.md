# JOBKREATORS — Design & Brand System

## Brand colors (measured from the source logo)

Sampled directly from `public/brand/jk-logo-original.jpg` with sharp (raw pixel
analysis, not eyeballed):

| Token            | Hex       | Where it appears in the logo                  | Notes |
| ---------------- | --------- | --------------------------------------------- | ----- |
| `--brand-navy`   | `#152A37` | Cube outline, tie, "JOBKREATORS" wordmark     | Dominant ink. Darkest pixel measured `#00121B`. |
| `--brand-cyan`   | `#5B9FBC` | Tie stripe + "RECRUITMENT AND CONSULTANCY"    | Accent. Most common quantized bucket `#60A8C0`. |

> **Brand decision (v2 assets, 2026-07-10 — supersedes the Phase 2/7 cream-tile
> mechanism):** The client supplied a proper high-resolution logo family with the
> **original brand colours preserved**, in two **surface-specific** variants per
> lockup/mark. The logo is **never recolored** and now sits **directly on the page
> background** — there is **no cream tile, no CSS filter, no `surface` prop**.
> `<Logo>` renders BOTH variants and a `dark:` visibility swap (keyed on
> `<html data-theme>`) reveals the correct one per theme — SSR-perfect, zero JS,
> zero flicker. The v1 rasters (the old bare/cream-tile lockup, the mark, the
> `-light` recolors, and the now-removed wordmark) are retained under
> `public/brand/_old_v1/` but are **not used by any component.**
>
> **Naming convention (conventional artwork-colour):** `-light` = **navy**
> artwork, rendered on **light** surfaces; `-dark` = **pale/cyan** artwork,
> rendered on **dark** surfaces — so the filename matches both the artwork colour
> *and* its target surface. (The v2 files were delivered with the two colours
> swapped relative to this convention; they were renamed on 2026-07-10 so the
> names are correct.)

Brand colour anchors (the cream logo tile is retired with v2 — the token remains
for UI surfaces, not the logo):

| Token              | Hex       | Use today |
| ------------------ | --------- | --------- |
| `--brand-cream`    | `#F4F5F0` | UI surface token (`--color-surface-2`); the logo cream tile is retired |
| `--brand-cyan-2`   | `#7CD4EC` | Bright-cyan UI accent (`--color-accent-bright`); not the logo |

> These become the `--brand-*` tokens in Phase 2 (`app/globals.css`). The current
> stylesheet still uses an unrelated `#0066FF` blue everywhere — Phase 2 replaces
> that with the real brand navy/cyan.

## Brand asset family

All generated by `scripts/generate-brand-assets.mjs` from the single JPEG source.
**Re-run `node scripts/generate-brand-assets.mjs` whenever the source logo
changes.** Never reference the raw JPEG in a component — always use these PNGs.

**v2 in-page logo assets** (client-supplied 2026-07-10, transparent PNG, rendered
by `<Logo>`; normalised to a 1600×1140 box for the lockup so the light/dark pair
share one box):

| File                    | Size       | Purpose |
| ----------------------- | ---------- | ------- |
| `jk-lockup-light.png`   | 1600×1138  | Full lockup, **navy** artwork. Rendered on **light** surfaces. |
| `jk-lockup-dark.png`    | 1600×1141  | Full lockup, **pale/cyan** artwork. Rendered on **dark** surfaces. |
| `jk-mark-light.png`     | 1024×1024  | Symbol only (cube + tie), **navy**. Light surfaces. |
| `jk-mark-dark.png`      | 1024×1024  | Symbol only, **pale/cyan**. Dark surfaces. |

**Favicon family** (unchanged v1 — retained deliberately; see note below):

| File                    | Size       | Purpose |
| ----------------------- | ---------- | ------- |
| `jk-mark-512.png`       | 512×512    | Symbol on `--brand-navy` tile, PWA icon (`app/manifest.ts`). |
| `jk-mark-180.png`       | 180×180    | Symbol on navy, Apple touch icon (`app/apple-icon.png`). |
| `jk-mark-32.png`        | 32×32      | Favicon (`app/icon.png`). |
| `jk-mark-16.png`        | 16×16      | Favicon fallback. |

> **Favicon decision (2026-07-10):** the v2 mark shipped **navy on transparent**,
> which vanishes on dark browser-tab chrome we don't control. So the favicon
> **stays on the v1 white-mark-on-navy-tile** family above (visible on any
> chrome); only the in-page lockup/mark moved to the v2 transparent assets. The
> mis-coloured v2 favicon rasters are archived in `public/brand/_old_v1/`.

## How the generator works

1. **Frame removal.** The source JPEG has a thin (~3px) gray rectangle around the
   artwork. The script insets `FRAME_INSET = 8px` on every edge before processing,
   then tight-trims, so no frame survives into the PNGs.
2. **Real alpha conversion.** This is a flat JPEG, so transparency is genuinely
   computed, not tagged. Per the brief, any pixel whose R, G and B are all above
   245 is flooded fully transparent. A short alpha *ramp* between min-channel 246
   (clear) and 220 (solid) is added below that hard cut — this removes the white
   halo that a pure hard threshold leaves on anti-aliased edges.
3. **Symbol / wordmark split.** Rather than hardcoding crop boxes, the script
   finds the **largest interior empty horizontal band** in the trimmed artwork
   (the gap between the cube and the wordmark) and splits there. This keeps working
   if the source logo is re-exported at a different size.
4. **Light recolor (pixel-based, chosen over CSS filter).** See below.

## Recolor approach — and why pixel-based won

The brief offered two options for the dark-surface variants: per-pixel recolor, or
a CSS `filter` fallback in the `<Logo />` component. **We use per-pixel recolor**
because it gives a noticeably cleaner, more controllable result here:

- A CSS `invert()/hue-rotate()` on the original logo would turn the navy into an
  arbitrary hue and can't independently preserve the cyan accent — the tie stripe
  and tagline would drift to the wrong color.
- Pixel recolor lets us map the two ink families independently: **navy → cream,
  cyan → brightened cyan**, with the cyan stripe kept intact.

**The non-obvious gotcha:** the brand navy `#152A37` is itself a *dark teal* — its
blue channel (0x37) already exceeds its red (0x15). So "blue is higher than red"
alone misclassifies the entire navy logo as cyan (the first run tinted everything
blue). The fix: the cyan accent is also **bright** (max channel ~190) while navy
ink is **dark** (max channel ~55). The classifier therefore requires *both* blue
dominance (`b - r > 28`) *and* brightness (`max(r,g,b) > 120`). Alpha is preserved
so edges stay smooth.

### Superseded by v2 client assets (2026-07-10)

> The former "known limitation / future ask" here — asking the client to supply a
> **clean vector (SVG)** for a pixel-perfect recolor — is **resolved.** On
> 2026-07-10 the client delivered **Version 2 assets** directly: high-resolution,
> **original brand colours preserved**, two surface variants per lockup/mark
> (light-surface = navy, dark-surface = pale/cyan), transparent PNG. **These v2
> assets are the canonical brand family.** The pixel-recolor pipeline below and
> the CSS-filter fallback are no longer used by the live site; they remain
> documented only for regenerating from a raw JPEG if the source ever changes.

### Sharpening passes (applied in the generator)

Because the source JPEG is only ~31 KB, every downscale re-encodes soft edges.
`scripts/generate-brand-assets.mjs` therefore applies two passes after trim +
alpha conversion, before the final PNG write:

1. **Upscale-then-downscale.** When the trimmed artwork's long edge is below
   `UPSCALE_FLOOR` (2000px), it is first upscaled to ~2000px with the `lanczos3`
   kernel, then resized down to the target. Sampling from a crisper raster
   reduces the softness of going straight to size.
2. **Unsharp mask.** A mild `.sharpen({ sigma: 0.8, m1: 1, m2: 2 })` on the way
   out crisps the edges without ringing.

> **Resolved 2026-07-10:** the earlier Phase 8 ask (client to supply a clean
> vector for a pixel-perfect logo) is closed — the client delivered v2
> high-resolution transparent PNGs directly. The generator's sharpening passes
> above apply only when regenerating from the raw JPEG; the live site uses the v2
> assets as-is.

## Design system (Phase 2 foundation)

The full token set lives in **one `@theme` block** in `app/globals.css` — this
is the Tailwind v4 way; there is **no `tailwind.config.js`**. Tokens are emitted
as `:root` custom properties and, within recognized namespaces (`--color-*`,
`--text-*`, `--radius-*`, `--shadow-*`, `--font-*`, `--ease-*`), generate the
matching utilities (each utility compiles to `var(--token)`).

> **Dual-theme — LIGHT default, DARK alternative (Phase 7, reversing the Phase 3
> dark-only decision).** The client's brand collateral lives on light surfaces, so
> light is canonical and dark is the deliberate alternative. `next-themes` drives
> a `data-theme` attribute on `<html>` (`defaultTheme="light"`,
> `enableSystem={false}` — a brand Sun/Moon toggle, not OS-following,
> `disableTransitionOnChange`).
>
> **How the swap works in Tailwind v4** (the key detail: color utilities only
> generate from `--color-*` declared **inside `@theme`**, and each compiles to
> `var(--token)`, so a token's *value* can be overridden at runtime):
>
> 1. **`@theme`** holds every token with its **LIGHT** value — the default cascade
>    is light, and all utilities exist.
> 2. **`:root[data-theme="dark"]`** overrides only the swapping color + elevation
>    tokens with their dark values (the previous dark-only palette, unchanged — so
>    dark cannot regress).
> 3. **`@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *))`**
>    makes `dark:` utilities respond to the toggle (not `prefers-color-scheme`).
>    Used sparingly: navbar blur depth, matching-engine stat chips, `.glass`, the
>    logo tile.
>
> **Accent is theme-split.** Light `--color-accent = #1C7C99` (a deeper cyan —
> bright `#7CD4EC` washes out on white; `#1C7C99` clears WCAG AA both as small
> text on `--color-bg` `#FAFAF7` at **4.56:1** and as white-on-accent fill at
> **4.77:1**). Dark `--color-accent = #7CD4EC` (bright). A shared
> **`--color-accent-bright = #7CD4EC`** "pop" tier is used in BOTH themes for
> small live signals (eyebrow status dot, match-line pulse, shortlist cluster).
>
> **Elevation is dual.** Light shadows are soft navy-tinted
> (`rgba(21,42,55,…)`, no inner highlight); dark shadows are rgba-black with an
> `inset` white highlight. `--shadow-glow-accent` is accent-tinted per theme.
>
> **Light text-subtle** was set to `#616D7B` (5.0:1 on `--color-bg`); the warmer
> `#6F7A88` measured only 4.18:1 and failed AA.

- **Color tiers.** `--color-brand-*` are the sacred logo colors. `--color-accent`
  (`#7CD4EC`), `--color-accent-2` (`#3DC6E8`) and `--color-accent-fg` are the
  screen-native UI accent, with a full `--color-accent-50…950` ramp for
  gradients, ring states and glow. Surfaces, borders, text and semantic colors
  follow. After this phase, **reference tokens, never literal hex.**
- **Type.** Body = **Inter** (`--font-sans`), display = **Space Grotesk**
  (`--font-display`), both loaded via `next/font/google` in `app/layout.tsx`. The
  modular scale (`text-display-2xl` … `text-eyebrow`) carries size, line-height,
  tracking and weight. Display-tier steps have a `-md` companion token
  (`text-display-2xl-md`, etc.) for the `md:` scale-up, e.g.
  `class="text-display-2xl md:text-display-2xl-md"`.
- **Spacing** uses the Tailwind v4 default scale (4/8/12/16/24/32/48/64/96/128px
  already map to `1/2/3/4/6/8/12/16/24/32`), so no extension is needed.
- **Radius / elevation / motion** are tokenized (`--radius-*`, dark-first
  `--shadow-*` including `--shadow-glow-accent`, and `--duration-*` / `--ease-*`).

### Motion safety — the canonical primitive

`lib/hooks/useMotionSafe.ts` exports **`useMotionSafe()`**, which returns `false`
when `prefers-reduced-motion: reduce` is set and `true` otherwise (SSR-safe:
renders `false`, resolves on mount, reacts live to OS changes). **This is the
canonical motion-safety primitive going forward** — every framer-motion
component must gate its animation on it:

```tsx
const motionSafe = useMotionSafe();
<motion.div animate={motionSafe ? { y: 0 } : false} … />
```

CSS animations are handled separately by the global
`@media (prefers-reduced-motion: reduce)` block in `app/globals.css`.

### UI primitives

Every section is built from these (in `components/ui/` unless noted):

- **`<Logo />`** (`components/Logo.tsx`) — the **only** way the logo appears.
  `variant` **mark/lockup** (the `wordmark` variant was removed with v2 — the
  client shipped no wordmark-only asset; consumers use `lockup` or `mark`), `size`
  sm/md/lg/xl or a px height, `priority`. **v2 (2026-07-10):** renders the
  surface-specific original-colour assets — `jk-{variant}-light.png` (navy) on
  light, `jk-{variant}-dark.png` (pale/cyan) on dark — **directly on the page
  background, no cream tile, no recolor, no `surface` prop**. It renders BOTH
  variants and a `dark:` visibility swap (`block dark:hidden` / `hidden
  dark:block`, keyed on `<html data-theme>`) reveals exactly one. Pure CSS, so the
  component stays a **Server Component** — SSR-correct in both themes, no
  `useTheme`, no mount flicker, no layout shift (both variants share a normalised
  1600×1140 lockup box). `priority` is applied to the light variant only
  (defaultTheme="light"). The v1 rasters live in `public/brand/_old_v1/`, unused.
- **`<Button />`** — variants primary/secondary/ghost/link, sizes sm/md/lg,
  `icon`, `loading`. Primary has a magnetic hover gated on `useMotionSafe()`.
  (`asChild` is omitted because `@radix-ui/react-slot` is not installed; the
  component forwards a ref instead.)
- **`<Section />`** — vertical rhythm (`py-24 md:py-32`) + optional `surface`.
- **`<Container />`** — `max-w-7xl`, centered, `px-6 md:px-8`, optional `size`.
- **`<Eyebrow />`** — uppercase label on the `eyebrow` token, optional pulsing
  accent status dot.
